spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: journalDB
      auto-index-creation: true



server:
  port: ${PORT:8080}
  servlet:
    context-path: /Journal

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /docs